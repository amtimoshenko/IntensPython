#include <iostream>
#include <math.h>
#include <cmath>

using namespace std;

int main() 
{
	int n_max = 10;
	double sum = 0.0;
	for (int i =3; i < n_max + 1; i++)
	{
		sum = (1.0 / ( i * i - 4));
	}
	cout << "Summa: " << sum;
	return 0;
}
