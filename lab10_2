def sum():
    n=input("Введите элементы массива через пробел: ")
    nums=[int(x) for x in n.split()]
    total=0
    for num in nums:
        if num==0:
            break
        total+=num
    return total
res=sum()
print("Сумма элементов массива до первого нуля: ", res)
