#include <iostream>
#include <math.h>
#include <cmath>

using namespace std;

int main() 
{
	int n_max = 10;
	double sum = 0.0;
	for (int i =1; i < n_max + 1; i++)
	{
		sum = (1.0 / ( 36*i * i - 24*i-5));
	}
	cout << "Summa: " << sum;
	return 0;
}
